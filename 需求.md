# Providers 项目

提供多个功能。每个功能有多个供应商。每个供应商重试2次（共请求3次），间隔1秒。

## 运行环境

- Cloudflare Workers

## 功能1: /v1/chat/completions

- OpenAI 兼容接口
- 提供多个供应商。 每个供应商代码中直接写死 base url 和 model 。 api key 由环境变量文件定义。
- 每个供应商提供属性: 是否支持 system prompt, 是否支持 tools, 是否支持 response format json_object, 是否支持 response format json_schema 。
- 目前仅实现非流式。

## 功能2: 读取页面

- 供应商: jina, tavily, firecrawl

## 功能3: 搜索

- 本次不实现
